# Copying this file is optional; Docker Compose reads these defaults directly.
# Generate the referenced secret files with scripts/Initialize-DevelopmentConfiguration.ps1.
SNOWSHOT_API_SECRETS_DIRECTORY=./.secrets/development/api
SNOWSHOT_MIGRATOR_SECRETS_DIRECTORY=./.secrets/development/migrator
POSTGRES_PASSWORD_FILE=./.secrets/development/postgres-password
TABLE_WORKER_URL=http://host.docker.internal:18080/
